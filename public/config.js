/* =========================================================================
   접속 설정
   -------------------------------------------------------------------------
   CA_SERVER 를 비워두면 페이지를 서빙한 곳과 같은 주소로 접속합니다.
   (Render 에 올린 주소로 들어왔다면 그대로 두면 됩니다.)

   GitHub Pages 처럼 정적 호스팅에서 클라이언트만 서빙하는 경우에는
   아래에 Render 서버 주소를 적어주세요. 예:

       window.CA_SERVER = 'https://crazyarcade-xxxx.onrender.com';

   비워두면 그 환경에서는 온라인 메뉴가 "서버 주소가 설정되지 않았습니다"
   안내를 띄우고, 싱글플레이는 정상 동작합니다.
   ========================================================================= */
window.CA_SERVER = '';
